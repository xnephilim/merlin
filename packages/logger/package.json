{
  "name": "@xblackfury/logger",
  "version": "2.0.0",
  "description": "A simple JSON logger with no external dependencies",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "directories": {
    "lib": "dist",
    "src": "src"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=8.11.3"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rm -rf dist && rm -rf tsconfig.build.tsbuildinfo",
    "compile": "tsc -p tsconfig.build.json",
    "dev": "tsc --watch",
    "prepare": "yarn build",
    "test": "jest --coverage"
  },
  "author": "",
  "license": "ISC"
}
